# ─── Database ──────────────────────────────────────────
#  DATABASE_URL=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=true&serverTimezone=UTC
# DATABASE_USERNAME=root
# DATABASE_PASSWORD=moSPDLviSDojItEQlZLDVAmlbTrkBdls
# JWT_SECRET=btechhub-secret-key-must-be-at-least-32-characters-long
# PORTt=8080


# ─── JPA / Hibernate ───────────────────────────────────
# spring.jpa.hibernate.ddl-auto=update
# 'update' means:
#   - First run  → creates all tables automatically from @Entity classes
#   - Later runs → only adds new columns if you added them, never deletes data
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# ─── Server ────────────────────────────────────────────
server.port=${PORT}

# ─── JWT ───────────────────────────────────────────────

# jwt.expiration=86400000
# expiration = 24 hours in milliseconds
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


jwt.secret=${JWT_SECRET}
jwt.expiration=86400000